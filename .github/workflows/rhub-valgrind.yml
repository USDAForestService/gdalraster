# Workflow derived from https://github.com/r-lib/actions/tree/v2/examples
# Need help debugging build failures? Start at https://github.com/r-lib/actions#where-to-find-help
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

name: rhub-valgrind

permissions: read-all

jobs:
  R-CMD-check-valgrind:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/r-hub/containers/valgrind:latest

    name: Valgrind

    strategy:
      fail-fast: false

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes

    steps:
      - uses: actions/checkout@692973e3d937matrix.config.label129bcbf40652eb9f2f61becf3332 # v4.1.7

      - uses: r-lib/actions/setup-pandoc@e6be4b3706e0f39bc7a4cf4496a5f2c4cb840040 # v2.10.1

      - uses: r-lib/actions/setup-r-dependencies@e6be4b3706e0f39bc7a4cf4496a5f2c4cb840040 # v2.10.1
        with:
          extra-packages: any::rcmdcheck
          needs: check

      - uses: r-hub/actions/run-check@6dfa5aa10532f35c73d10e7f0b10af8a1340de3e # v1.2.1
        with:
          matrix.config.label: 'valgrind'
